import WorkspacePlaceholder from '../components/WorkspacePlaceholder'

export default function Transactions() {
  return <WorkspacePlaceholder eyebrow="Evidence browser" title="Transactions" description="Inspect query-scoped transaction evidence and linked counterparties." phase="Phase B" />
}
